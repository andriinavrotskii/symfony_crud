
<div class="container">
    {#
    <div class="row">
        {{ form_start(form) }}
        <div class="input-group placeholder-bold {{ form_errors(form.name) ? 'has-danger' : '' }}">
            {{ form_widget(form.name, {attr: {'class' : 'form-control', 'placeholder': 'Name'|trans|striptags ~ '*'}}) }}
            {%  if form_errors(form.name) %}
                <div class="form-control-feedback">
                    {{  form_errors(form.name) }}
                </div>
            {%  endif %}
        </div>
        <div class="input-group placeholder-bold {{ form_errors(form.phone) ? 'has-danger' : '' }}">
            {{ form_widget(form.phone, {attr: {'class' : 'form-control', 'placeholder': 'phone'|trans|striptags ~ '*'}}) }}
            {%  if form_errors(form.phone) %}
                <div class="form-control-feedback">
                    {{  form_errors(form.phone) }}
                </div>
            {%  endif %}
        </div>
        <div class="input-group placeholder-bold {{ form_errors(form.email) ? 'has-danger' : '' }}">
            {{ form_widget(form.email, {attr: {'class' : 'form-control', 'placeholder': 'email'|trans|striptags ~ '*'}}) }}
            {%  if form_errors(form.email) %}
                <div class="form-control-feedback">
                    {{  form_errors(form.email) }}
                </div>
            {%  endif %}
        </div>
        <div class="input-group placeholder-bold {{ form_errors(form.text) ? 'has-danger' : '' }}">
            {{ form_widget(form.text, {attr: {'class' : 'form-control', 'placeholder': 'text'|trans|striptags ~ '*'}}) }}
            {%  if form_errors(form.text) %}
                <div class="form-control-feedback">
                    {{  form_errors(form.text) }}
                </div>
            {%  endif %}
        </div>
        {{ form_end(form) }}
    </div>
    #}


    <form>
        <input type="hidden" name="app_bundle_message_type[_token]" value="{{ csrf_token('app_bundle_message_type') }}">
        <div class="row">
            <div class="col-6">
                <div class="form-group name">
                    <input class="form-control" type="text" placeholder="Name" name="app_bundle_message_type[name]">
                </div>
                <div class="form-group phone">
                    <input class="form-control" type="text" placeholder="Phone" name="app_bundle_message_type[phone]">
                </div>
                <div class="form-group email">
                    <input class="form-control" type="email" placeholder="Email" name="app_bundle_message_type[email]">
                </div>
            </div>
            <div class="col-6">
                <div class="form-group text">
                    <textarea class="form-control" rows="3" placeholder="Texts" name="app_bundle_message_type[text]"></textarea>
                </div>
                <button type="submit" class="btn btn-primary submit-form">Save</button>
            </div>
        </div>
    </form>

</div>
